const darkTheme = {
  mainColor: '#fff',
  secColor: '#eee',
  bgColor: '#282a35',
  bgButtonColor: '#fff',
  borderColor: '#c1c1c1'
}

export default darkTheme

export type Theme = {
  mainColor: string
  secColor: string
  bgColor: string
  bgButtonColor: string
  borderColor: string
}
